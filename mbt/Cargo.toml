[package]
name = "ariadne-mbt"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
ariadne-analysis = { path = ".." }
mirrorrust = { path = "../../MirrorRust" }
num-bigint = "0.4"
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
